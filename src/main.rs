//use std::fs;

fn main() {
    env_logger::init();
    println!("EMU 6502");

    //let code = fs::read_to_string("code.6502")
    //    .expect("Failed to read code");

    //let program = emu6502::assemble(code)
    //    .expect("Failed to assemble the code");
    //let code = include_bytes!("tests/asm/ex01.o65");
}
